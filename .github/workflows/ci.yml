name: CI

on:
  pull_request:
  push:
    branches:
      - main
      - "codex/**"

jobs:
  test:
    name: Backend and frontend checks
    runs-on: ubuntu-latest
    timeout-minutes: 20
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: qrant
          POSTGRES_PASSWORD: qrant_dev
          POSTGRES_DB: qrant_agent
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U qrant -d qrant_agent"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 20
      redis:
        image: redis:7
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 20
    env:
      DATABASE_URL: postgresql://qrant:qrant_dev@127.0.0.1:5432/qrant_agent
      REDIS_URL: redis://127.0.0.1:6379/0
      MARKETLENS_MARKET_PROVIDER: free
      MARKETLENS_HISTORY_PROVIDER: akshare
      MARKETLENS_INFO_PROVIDER: akshare
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"
          cache: pip
          cache-dependency-path: backend/requirements.lock

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm

      - name: Install Python dependencies
        run: python -m pip install -r backend/requirements.lock

      - name: Install Node dependencies
        run: npm ci

      - name: Apply database migrations
        run: python -m backend.app.migrations

      - name: Run backend tests
        run: python -m pytest backend/tests -q

      - name: Compile Python modules
        run: python -m compileall -q backend tools

      - name: Build frontend
        run: npm run build
